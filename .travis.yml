language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-wakanda-extension' ]; then cd .. && eval "mv $currentfolder generator-wakanda-extension" && cd generator-wakanda-extension; fi

